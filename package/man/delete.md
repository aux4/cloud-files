#### Description

Deletes one of the authenticated user's files from the scope's managed `files` VM. The command asks for confirmation before deleting.

#### Usage

```bash
aux4 cloud files delete <path>
```

#### Example

```bash
aux4 cloud files delete notes/today.txt
```
