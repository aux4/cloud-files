# aux4 platform app files ui

Return the Files application routes, API actions, and responsive file-manager component bundle.

The Files UI provides breadcrumb navigation, current-folder search, drag-and-drop or picker uploads, folder creation, and confirmed file deletion. Initial loads use stable row placeholders; background refreshes leave rendered file rows in place and show a thin progress indicator without layout shift.
