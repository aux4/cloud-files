#### Description

Lists the authenticated user's files in the scope's managed `files` VM. The VM name and runtime configuration are fixed by the package.

#### Usage

```bash
aux4 cloud files list
```

#### Example

```bash
aux4 cloud files list
```
