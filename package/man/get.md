#### Description

Reads a text file belonging to the authenticated user from the scope's managed `files` VM.

#### Usage

```bash
aux4 cloud files get <path>
```

#### Example

```bash
aux4 cloud files get notes/today.txt
```
